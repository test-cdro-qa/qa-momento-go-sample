
apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: pending_tasks_no_approver
on:
  workflow_dispatch:
jobs:
  ask-now:
    delegates: cloudbees-io/manual-approval/custom-job.yml@v1
    timeout-minutes: 25
    with:
      instructions: ListPendingTasks API test - no explicit approvers.
      debug: true