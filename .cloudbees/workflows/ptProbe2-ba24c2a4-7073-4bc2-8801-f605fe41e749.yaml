apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: pending_tasks_probe
on:
  workflow_dispatch:
jobs:
  ask-now:
    delegates: cloudbees-io/manual-approval/custom-job.yml@v1
    timeout-minutes: 20
    with:
      instructions: Case B probe.
      approvers: AutoTest.5791d56a-126b-4f19-a5ce-7ebd502904ef.PendingTaskTeam
      debug: true